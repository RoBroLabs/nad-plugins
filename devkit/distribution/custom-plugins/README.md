# Custom plugins

Create Apps, Add-ons, Widgets and pages in this directory using the scripts in
the Devkit root. Each child directory is an independent NAD package and is
covered by the workspace's local SDK overrides.

Do not copy first-party plugin source into this directory. Start from a current
scaffold so the package declares compatible Host API and UI API contracts.
