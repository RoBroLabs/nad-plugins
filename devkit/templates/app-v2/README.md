# __PACKAGE_NAME__

Schema-v2 NAD App scaffold for `__PACKAGE_ID__`.

Named connection profiles are owned and encrypted by NAD core. The server receives only the profile selected for the current invocation. The Widget is self-contained and communicates through UI bridge v2.

Run `pnpm build`, `pnpm test`, and `pnpm check` before packing.
