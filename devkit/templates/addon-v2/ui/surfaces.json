{
  "schemaVersion": 2,
  "surfaces": [
    {
      "id": "app-summary",
      "kind": "widget",
      "name": "__PACKAGE_NAME__",
      "description": "Summary returned by the selected App connection without exposing its credentials.",
      "entry": "ui/surfaces/app-summary.html",
      "bridge": "2.x",
      "permissions": ["view"],
      "connectionSlots": [
        { "slot": "primary", "target": "app", "required": true }
      ],
      "bindings": {
        "summary": { "target": "app", "operation": "summary", "connectionSlot": "primary" }
      },
      "widget": {
        "defaultSize": { "w": 4, "h": 3 },
        "minSize": { "w": 3, "h": 2 },
        "maxSize": { "w": 8, "h": 6 },
        "chrome": "standard"
      },
      "execution": {
        "requestedMode": "sandbox",
        "privileges": ["theme", "resize", "connection-selection"]
      }
    }
  ]
}
