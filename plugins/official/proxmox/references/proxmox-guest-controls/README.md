# Proxmox Guest Controls Add-on

This UI-only schema-v2 Add-on proves that a separately installed Widget can consume versioned Proxmox App operations while NAD core retains the connection configuration, token secret, permission enforcement and audit boundary.
