{
  "name": "@nad/module-proxmox",
  "version": "1.0.2",
  "private": true,
  "description": "Official NAD Proxmox Module.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "nad-module check .",
    "preview": "pnpm build && nad-module dev .",
    "preview:once": "pnpm build && nad-module dev . --once --role viewer --endpoint overview",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "@nad/cli": "workspace:*",
    "@nad/sdk": "workspace:*",
    "@nad/testkit": "workspace:*"
  }
}
