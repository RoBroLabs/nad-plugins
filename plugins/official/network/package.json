{
  "name": "@nad/module-network",
  "version": "1.0.0",
  "private": true,
  "description": "Official NAD Network and Pi-hole Module.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "nad-module check .",
    "preview": "pnpm build && nad-module dev .",
    "preview:once": "pnpm build && nad-module dev . --once --role viewer --endpoint stats",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "@nad/cli": "workspace:*",
    "@nad/sdk": "workspace:*",
    "@nad/testkit": "workspace:*"
  }
}
